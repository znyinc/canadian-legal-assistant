{
  "name": "canadian-legal-assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.6.3",
    "vitest": "^1.6.0"
  }
}
