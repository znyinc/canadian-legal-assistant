import { EvidenceIndex, EvidenceManifest, SourceEntry, SourceManifest } from '../models';
export declare class ManifestBuilder {
    buildSourceManifest(entries: SourceEntry[], notes?: string[]): SourceManifest;
    buildEvidenceManifest(index: EvidenceIndex, notes?: string[]): EvidenceManifest;
}
//# sourceMappingURL=ManifestBuilder.d.ts.map